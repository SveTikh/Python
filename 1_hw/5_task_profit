profit = float(input ('Введите выручку компании:  '))
cost = float(input ('Введите издержки компании:  '))

if profit > cost:
    print(f"Компания работает с прибылью {profit / cost:.2f}")
    count_of_persons = int(input ('Введите численность сотрудников фирмы:  '))
    print(f"Прибыль в расчете на одного сторудника сотавила {profit / count_of_persons:.2f}")

elif profit < costs:
    print("Компания работает в убыток")

else:
    print("Фирма работает в ноль")
